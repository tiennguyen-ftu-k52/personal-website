import React from 'react';
import {compose, withHandlers} from 'recompose';
import {Container} from 'semantic-ui-react';

import {StyledSegment} from '../ui';

const Others = () => (
  <StyledSegment>

  </StyledSegment>
);

export default compose(

)(Others);